import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
	btnContainer: {
		flex: 1,
	},
	btnStyle: {
		borderRadius: 15,
		marginBottom: 5,
		marginHorizontal: 5,
		alignItems: "center",
	},
})

export default styles
