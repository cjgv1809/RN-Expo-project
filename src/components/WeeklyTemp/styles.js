import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
	weatherWeekList: {
		alignItems: "center",
		flex: 1,
		marginLeft: 7,
		marginTop: 10,
	},
	textTemWeek: {
		color: "#fff",
		fontSize: 22,
	},
})

export default styles
