import React from "react";
import { View, Text, StyleSheet, TouchableOpacity } from "react-native";

const MapMyCitiesScreen = () => {
  return (
    <View style={styles.view}>
      <Text>Map</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default MapMyCitiesScreen;
